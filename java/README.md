# Java — 35 ejercicios

Código **ejecutable** en Java (35 clases + menú `Main.java`).

```bash
cd java/src
javac *.java
java Main
```

El catálogo web con demos y ejemplos en JavaScript y Python está en la raíz del repositorio.
