{
  "name": "ejercicios-practica-web",
  "version": "1.0.0",
  "description": "35 programming practice exercises — Java, JavaScript, Python",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "build:ghpages": "BASE_PATH=/Excercises_practice/ vite build",
    "preview": "vite preview",
    "deploy": "npm run build:ghpages && gh-pages -d dist"
  },
  "engines": {
    "node": ">=18.0.0"
  },
  "devDependencies": {
    "gh-pages": "^6.3.0",
    "vite": "^6.2.0"
  }
}
